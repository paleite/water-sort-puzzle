# Architecture & State

- Features live under `app/`; reusable UI in `components/`.
- Keep functions pure, small, and descriptively named.
- Use Zustand for state management; add persist middleware when appropriate.
